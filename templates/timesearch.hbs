    <div class="accordion">
        <h4 class ="handle">
            <a href="#">{{name}} (Orlando, Florida)
            </a>
        </h4>
        <div>{{description}}
            <br>
            {{#if times}}{{times}}
            {{/if}}
        </div>
    </div>

