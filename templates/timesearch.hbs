    <div class="accordion">
        <h4 class ="handle">
            <a href="#">{{name}} ({{location}})
            </a>
        </h4>
        <div>{{description}}
            <br>
            {{#if times}}{{times}}
            {{/if}}
        </div>
    </div>

